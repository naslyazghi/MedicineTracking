import React from 'react';
import { View, Text, Button, TouchableOpacity, Dimensions, StyleSheet, StatusBar, Image } from 'react-native';



const SearchEachesResultsScreen = ({route, navigation}) => {
   // let {search_param} = route.params;
    return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>Search Eaches Results Screen</Text>
    </View>
    );
};


export default SearchEachesResultsScreen;

